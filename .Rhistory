getwd()
df<- read.csv("data//mRNA_expr.csv")
head(df,5)
library("tidyr")
long_df <- df %>% pivot_longer(!c(bcr_patient_barcode, dataset), names_to = "Genes de expresión mRNA", values_to = "valores")
# Genere la data df_long
df_long <- long_df[,-1]
View(df_long)
# Escriba el código para crear el Descriptive table
# Escriba el código para crear la función tcga_boxplots
install.packages(sjPlot)
# Escriba el código para generar la lista.
# Escriba el código para guardar la tercera figura de la lista.
# Escriba aquí su función reg_gen_expression
# Escriba el código para generar la lista.
# Escriba el código para guardar el onceavo plot.
getwd()
df<- read.csv("data/mRNA_expr.csv")
head(df,5)
library("tidyr")
long_df <- df %>% pivot_longer(!c(bcr_patient_barcode, dataset), names_to = "Genes de expresión mRNA", values_to = "valores")
# Genere la data df_long
df_long <- long_df[,-1]
View(df_long)
# Escriba el código para crear el Descriptive table
# Escriba el código para crear la función tcga_boxplots
install.packages(sjPlot)
# Escriba el código para generar la lista.
# Escriba el código para guardar la tercera figura de la lista.
# Escriba aquí su función reg_gen_expression
# Escriba el código para generar la lista.
# Escriba el código para guardar el onceavo plot.
```{r wd}
getwd()
df<- read.csv("data/mRNA_expr.csv")
head(df,5)
library("tidyr")
long_df <- df %>% pivot_longer(!c(bcr_patient_barcode, dataset), names_to = "Genes de expresión mRNA", values_to = "valores")
# Genere la data df_long
df_long <- long_df[,-1]
View(df_long)
# Escriba el código para crear el Descriptive table
# Escriba el código para crear la función tcga_boxplots
# Escriba el código para generar la lista.
# Escriba el código para guardar la tercera figura de la lista.
# Escriba aquí su función reg_gen_expression
# Escriba el código para generar la lista.
# Escriba el código para guardar el onceavo plot.
```{r wd}
getwd()
df<- read.csv("data/mRNA_expr.csv")
head(df,5)
library("tidyr")
long_df <- df %>% pivot_longer(!c(bcr_patient_barcode, dataset), names_to = "Genes de expresión mRNA", values_to = "valores")
# Genere la data df_long
df_long <- long_df[,-1]
View(df_long)
library(dplyr)
datos=descr(df_long%>%select(gen,dataset))
